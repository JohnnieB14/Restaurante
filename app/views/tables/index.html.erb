<% provide(:title, 'Mesas') %>
<div class="center hero-unit">
	<h1>Lista de mesas</h1>

	<br/>
	
	<table class="table table-bordered table-hover">
		<thead>
		<tr>
			<th>Id</th>
			<th>Código</th>
			<th>Mesero</th>
			<th>Editar</th>
			<th>Borrar</th>
		</tr>
		</thead>
		<tbody>	
			<% @tables.each do |table| %>
			  <tr>
				<td><%= table.id %></td>
				<td><%= table.code %></td>
			    <td><%= Waiter.find(table.waiter_id).name + ' ' + Waiter.find(table.waiter_id).surname %></td>
			    <td><%= link_to image_tag("edit.png", alt: 'Editar'), edit_table_path(table) %></td>
			    <td><%= link_to image_tag("delete.png", alt: 'Borrar'), table, method: :delete, data: { confirm: 'Estas seguro?' } %></td>
			  </tr>
			<% end %>
		</tbody>
	</table>

	<%= link_to 'Nueva Mesa', new_table_path , class: "justify"%>

	<br />
</div>
